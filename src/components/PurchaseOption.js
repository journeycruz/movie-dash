import React from 'react'

function PurchaseOption({ title }) {
    return (
        <div className='movie-container'>
            <h1>{ title }</h1>
        </div>
    )
}

export default PurchaseOption
